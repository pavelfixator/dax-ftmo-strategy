"""Strategy layer — indicators, market data feed, news, setup classes.

Pure-ish modules: market data ingestion + signal generation.
No risk sizing here (see src.risk), no order routing (see src.execution).
"""
